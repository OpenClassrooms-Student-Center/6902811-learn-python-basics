# Instructions  

1. **Create** a **dictionary** called `fruits` with keys `"apple"`, `"banana"`, and `"orange"`, and their respective values `"red"`, `"yellow"`, and `"orange"`.
2. **Add** the key `"kiwi"` with value `"green"` to the `fruits` dictionary.
3. **Access** the value corresponding to the key `"banana"` and store it in a variable named `banana_color`.
4. **Modify** the value associated with the key `"apple"` to `"green"`.
5. **Remove** the key `"banana"` from the `fruits` dictionary.
6. **Display** the remaining keys in the dictionary.
